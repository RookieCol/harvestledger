apiVersion: v2
name: harvestledger
description: HarvestLedger — NestJS microservices (gateway + auth/farms/tracing) with Postgres, MongoDB, Redis and RabbitMQ
type: application
version: 0.1.0
appVersion: "0.0.1"
