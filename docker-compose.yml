version: "3"
services:
  rabbitmq:
    image: "rabbitmq:management-alpine"
    container_name: rabbitmq
    hostname: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672" # Web management console
    env_file:
      - .env

  gateway:
    depends_on:
      - rabbitmq

    build:
      context: ./
      dockerfile: ./apps/gateway/Dockerfile
      target: builder # dev uses the builder stage (has the Nest CLI for start:dev)
    ports:
      - "8086:5000"
    volumes:
      - .:/usr/src/app # any change to base folder should be reflected
      - /usr/src/app/node_modules
    command: pnpm run start:dev gateway

  auth:
    build:
      context: ./
      dockerfile: ./apps/auth/Dockerfile
      target: builder
    depends_on:
      - rabbitmq
      - postgres-auth
      - minio
    environment:
      # Each service now owns its own database, so each safely runs its own
      # migrations (overrides the .env default).
      - DB_RUN_MIGRATIONS=true
    volumes:
      - .:/usr/src/app # any change to base folder should be reflected
      - /usr/src/app/node_modules
    command: pnpm run start:dev auth

  farms:
    build:
      context: ./
      dockerfile: ./apps/farms/Dockerfile
      target: builder
    depends_on:
      - gateway
      - rabbitmq
      - postgres-farms
      - minio
    environment:
      - DB_RUN_MIGRATIONS=true
    volumes:
      - .:/usr/src/app # any change to base folder should be reflected
      - /usr/src/app/node_modules
    command: pnpm run start:dev farms

  tracing:
    build:
      context: ./
      dockerfile: ./apps/tracing/Dockerfile
      target: builder
    depends_on:
      - rabbitmq
      - mongo
    volumes:
      - .:/usr/src/app # any change to base folder should be reflected
      - /usr/src/app/node_modules
    command: pnpm run start:dev tracing # overrides CMD from dockerfile

  postgres-auth:
    image: "postgres:latest"
    env_file:
      - ./.env
    environment:
      - POSTGRES_DB=${AUTH_POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ./pg_data_auth:/var/lib/postgresql/data

  postgres-farms:
    image: "postgres:latest"
    env_file:
      - ./.env
    environment:
      - POSTGRES_DB=${FARMS_POSTGRES_DB}
    ports:
      - "5433:5432"
    volumes:
      - ./pg_data_farms:/var/lib/postgresql/data

  postgres_admin:
    image: dpage/pgadmin4
    depends_on:
      - postgres-auth
      - postgres-farms
    env_file:
      - .env
    ports:
      - '15432:80'

  mongo:
    image: "mongo:7"
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./mongo_data:/data/db

  redis:
    image: "redis:7-alpine"
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis_data:/data

  minio:
    image: "minio/minio"
    container_name: minio
    command: server /data --console-address ":9001"
    env_file:
      - .env
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY_ID}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_ACCESS_KEY}
    ports:
      - "9000:9000" # S3 API
      - "9001:9001" # Web console
    volumes:
      - ./minio_data:/data

  minio_createbucket:
    image: "minio/mc"
    depends_on:
      - minio
    env_file:
      - .env
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc alias set local http://minio:9000 $${S3_ACCESS_KEY_ID} $${S3_SECRET_ACCESS_KEY}) do echo 'waiting for minio...' && sleep 1; done;
      /usr/bin/mc mb --ignore-existing local/$${S3_BUCKET};
      exit 0;
      "
