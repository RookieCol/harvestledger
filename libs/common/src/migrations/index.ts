import { InitialSchema1785028946448 } from './1785028946448-InitialSchema';

// Ordered list of migrations, imported as classes so they are bundled into
// each app's webpack build and available at runtime (migrationsRun).
export const migrations = [InitialSchema1785028946448];
